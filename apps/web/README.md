# Web (`@sailing-plans/web`)

Next.js App Router shell for local development.

```bash
# from repo root
pnpm --filter @sailing-plans/web dev
```

Defaults to [http://localhost:3000](http://localhost:3000) and reads `NEXT_PUBLIC_API_URL` (see `.env.example`).
